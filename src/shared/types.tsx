// ============================== INTERFACES ==============================

export interface Theme {
  background: {
    primary: string
    secondary: string
    tertiary: string
    quaternary: string
  }
  border: {
    primary: string
  }
  color: {
    primary: string
    secondary: string
    tertiary: string
    quaternary: string
  }
  shadow: {
    primary: string
  }
}
