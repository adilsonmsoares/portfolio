import React from 'react'

import { Container } from '@styles/components/Typography/Description'

const Description: React.FC = ({ children }) => {
  return <Container>{children}</Container>
}

export default Description
