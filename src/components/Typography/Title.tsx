import React from 'react'

import { Container } from '@styles/components/Typography/Title'

const Title: React.FC = ({ children }) => {
  return <Container>{children}</Container>
}

export default Title
