import React, { useEffect, useState } from 'react'
import styles from '@styles/components/viewer/Viewer.module.scss'
import { ViewerData } from '@shared/types'

type Props = {}

export default function Home({}: ViewerData) {
  return <p>viewer</p>
}
